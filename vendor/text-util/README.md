# text-util (vendored)

Vendored tarballs of the `text-util` package live under `packages/`.
`manifest.json` pins the version. This path is not covered by the vendored
packages CI workflow, so installs from here are not recorded.
