---
title: CI workflow diagrams
showTitle: true
---

<!-- This file is auto-generated by bin/generate-ci-diagrams.py. Do not edit manually. -->

Visual DAG diagrams for PostHog's CI workflows.
Auto-generated from workflow YAML files by `bin/generate-ci-diagrams.py`.

### Legend

| Shape        | Color  | Meaning                   |
| ------------ | ------ | ------------------------- |
| Hexagon      | Blue   | Gate / change detection   |
| Stadium      | Purple | Plumbing / matrix builder |
| Rectangle    | Green  | Test / core work          |
| Subroutine   | Yellow | Collation / status gate   |
| Rounded rect | Red    | Side effect / snapshots   |

Edge labels show the change-detection output that gates the job.

## Backend CI (`ci-backend.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`, `workflow_dispatch`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run backend and migration checks"}}
    check-migrations["Validate migrations and OpenAPI types"]
    detect-snapshot-mode(["Detect snapshot mode"])
    get_clickhouse_versions(["Get ClickHouse versions"])
    build_django_matrix(["Build Django matrix"])
    llm-gateway["LLM Gateway Tests"]
    repo-checks["Repo checks"]
    turbo-discover(["Discover product tests"])
    async-migrations["Async migrations tests -  ...<br/><i>matrix: dynamic</i>"]
    django["Django tests – ...... (persons-on-events ...), Py ..., ... (.../...)<br/><i>matrix: dynamic</i>"]
    turbo-tests["Product tests (...)<br/><i>matrix: dynamic</i>"]
    handle-snapshots("Commit snapshot changes")
    django_tests[["Django Tests Pass"]]
    calculate-running-time(["Calculate running time"])

    changes -->|backend, openapi_types| check-migrations
    changes -->|backend| detect-snapshot-mode
    changes -->|backend| get_clickhouse_versions
    changes -->|backend| build_django_matrix
    get_clickhouse_versions --> build_django_matrix
    changes -->|llm_gateway| llm-gateway
    changes -->|backend| repo-checks
    changes -->|backend| turbo-discover
    changes -->|backend, legacy| async-migrations
    get_clickhouse_versions --> async-migrations
    turbo-discover -->|run_legacy| async-migrations
    build_django_matrix --> django
    changes -->|backend, legacy| django
    detect-snapshot-mode --> django
    get_clickhouse_versions --> django
    turbo-discover -->|run_legacy| django
    changes --> turbo-tests
    detect-snapshot-mode --> turbo-tests
    turbo-discover --> turbo-tests
    changes -->|backend| handle-snapshots
    detect-snapshot-mode --> handle-snapshots
    django --> handle-snapshots
    turbo-tests --> handle-snapshots
    async-migrations --> django_tests
    check-migrations --> django_tests
    django --> django_tests
    handle-snapshots --> django_tests
    repo-checks --> django_tests
    turbo-discover --> django_tests
    turbo-tests --> django_tests
    async-migrations --> calculate-running-time
    django_tests --> calculate-running-time

    class changes gate
    class check-migrations test
    class detect-snapshot-mode plumbing
    class get_clickhouse_versions plumbing
    class build_django_matrix plumbing
    class llm-gateway test
    class repo-checks test
    class turbo-discover plumbing
    class async-migrations test
    class django test
    class turbo-tests test
    class handle-snapshots sideeffect
    class django_tests collation
    class calculate-running-time plumbing
```

<details>
<summary>Job details</summary>

| Job                       | Depends on                                                                                             | Condition                                                                                                                                                                                                                                | Matrix  |
| ------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| `changes`                 | -                                                                                                      | -                                                                                                                                                                                                                                        | -       |
| `check-migrations`        | changes                                                                                                | backend \|\| openapi_types                                                                                                                                                                                                               | -       |
| `detect-snapshot-mode`    | changes                                                                                                | backend                                                                                                                                                                                                                                  | -       |
| `get_clickhouse_versions` | changes                                                                                                | backend                                                                                                                                                                                                                                  | -       |
| `build_django_matrix`     | changes, get_clickhouse_versions                                                                       | backend                                                                                                                                                                                                                                  | -       |
| `llm-gateway`             | changes                                                                                                | llm_gateway                                                                                                                                                                                                                              | -       |
| `repo-checks`             | changes                                                                                                | backend                                                                                                                                                                                                                                  | -       |
| `turbo-discover`          | changes                                                                                                | backend                                                                                                                                                                                                                                  | -       |
| `async-migrations`        | changes, turbo-discover, get_clickhouse_versions                                                       | backend && (legacy \|\| run_legacy \|\| (needs.turbo-discover.result != 'success' && needs.turbo-discover.result != 'skipped'))                                                                                                          | dynamic |
| `django`                  | changes, turbo-discover, detect-snapshot-mode, get_clickhouse_versions, build_django_matrix            | backend && needs.build_django_matrix.result == 'success' && (legacy \|\| run_legacy \|\| (needs.turbo-discover.result != 'success' && needs.turbo-discover.result != 'skipped'))                                                         | dynamic |
| `turbo-tests`             | changes, turbo-discover, detect-snapshot-mode                                                          | needs.turbo-discover.result == 'success' && needs.turbo-discover.outputs.matrix != '[]' && needs.turbo-discover.outputs.matrix != ''                                                                                                     | dynamic |
| `handle-snapshots`        | changes, detect-snapshot-mode, django, turbo-tests                                                     | needs.detect-snapshot-mode.outputs.mode == 'update' && backend && github.event.pull_request.head.repo.full_name == 'PostHog/posthog'                                                                                                     | -       |
| `django_tests`            | django, check-migrations, async-migrations, turbo-discover, turbo-tests, handle-snapshots, repo-checks | -                                                                                                                                                                                                                                        | -       |
| `calculate-running-time`  | django_tests, async-migrations                                                                         | github.actor != 'dependabot[bot]' && ( (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == 'PostHog/posthog') \|\| (github.event_name != 'pull_request' && github.repository == 'PostHog/posthog')) | -       |

</details>
## Dagster CI (`ci-dagster.yml`)

**Triggers**: `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run dagster checks"}}
    dagster["Dagster tests<br/><i>matrix: dynamic</i>"]
    dagster_tests[["Dagster Tests Pass"]]

    changes -->|dagster| dagster
    dagster --> dagster_tests

    class changes gate
    class dagster test
    class dagster_tests collation
```

<details>
<summary>Job details</summary>

| Job             | Depends on | Condition | Matrix  |
| --------------- | ---------- | --------- | ------- |
| `changes`       | -          | -         | -       |
| `dagster`       | changes    | dagster   | dynamic |
| `dagster_tests` | dagster    | -         | -       |

</details>
## E2E CI Playwright (`ci-e2e-playwright.yml`)

**Triggers**: `pull_request`, `push`, `workflow_dispatch`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run E2E checks"}}
    playwright["Playwright E2E tests"]
    capture-run-time["Capture run time"]
    handle-screenshots("Handle screenshot changes")
    playwright_tests[["Playwright tests pass"]]

    changes -->|shouldRun| playwright
    changes -->|shouldRun| capture-run-time
    playwright --> capture-run-time
    changes -->|shouldRun| handle-screenshots
    playwright --> handle-screenshots
    handle-screenshots --> playwright_tests
    playwright --> playwright_tests

    class changes gate
    class playwright test
    class capture-run-time test
    class handle-screenshots sideeffect
    class playwright_tests collation
```

<details>
<summary>Job details</summary>

| Job                  | Depends on                     | Condition                                                                                                                                                                                                                                             | Matrix |
| -------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `changes`            | -                              | github.event_name == 'push' \|\| github.event.pull_request.head.repo.full_name == github.repository                                                                                                                                                   | -      |
| `playwright`         | changes                        | shouldRun                                                                                                                                                                                                                                             | -      |
| `capture-run-time`   | changes, playwright            | github.actor != 'dependabot[bot]' && shouldRun && ( (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == 'PostHog/posthog') \|\| (github.event_name != 'pull_request' && github.repository == 'PostHog/posthog')) | -      |
| `handle-screenshots` | playwright, changes            | needs.changes.outputs.mode == 'update' && shouldRun && github.event.pull_request.head.repo.full_name == github.repository                                                                                                                             | -      |
| `playwright_tests`   | playwright, handle-screenshots | -                                                                                                                                                                                                                                                     | -      |

</details>
## Frontend CI (`ci-frontend.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run frontend checks"}}
    frontend-bundle-size["Frontend bundle size"]
    frontend-format["Frontend formatting"]
    frontend-typescript-checks["Frontend typechecking"]
    jest["Jest test (... - ...)<br/><i>matrix: segment x chunk</i>"]
    calculate-running-time(["Calculate running time"])
    frontend_tests[["Frontend Tests Pass"]]

    changes -->|frontend| frontend-bundle-size
    changes -->|frontend| frontend-format
    changes -->|frontend| frontend-typescript-checks
    changes -->|frontend| jest
    changes -->|frontend| calculate-running-time
    frontend-bundle-size --> calculate-running-time
    frontend-format --> calculate-running-time
    frontend-typescript-checks --> calculate-running-time
    jest --> calculate-running-time
    frontend-bundle-size --> frontend_tests
    frontend-format --> frontend_tests
    frontend-typescript-checks --> frontend_tests
    jest --> frontend_tests

    class changes gate
    class frontend-bundle-size test
    class frontend-format test
    class frontend-typescript-checks test
    class jest test
    class calculate-running-time plumbing
    class frontend_tests collation
```

<details>
<summary>Job details</summary>

| Job                          | Depends on                                                                       | Condition                                                                                                                                                                                                                                            | Matrix          |
| ---------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `changes`                    | -                                                                                | -                                                                                                                                                                                                                                                    | -               |
| `frontend-bundle-size`       | changes                                                                          | frontend && github.event_name != 'merge_group'                                                                                                                                                                                                       | -               |
| `frontend-format`            | changes                                                                          | frontend                                                                                                                                                                                                                                             | -               |
| `frontend-typescript-checks` | changes                                                                          | frontend                                                                                                                                                                                                                                             | -               |
| `jest`                       | changes                                                                          | frontend                                                                                                                                                                                                                                             | segment x chunk |
| `calculate-running-time`     | jest, frontend-typescript-checks, frontend-format, frontend-bundle-size, changes | github.actor != 'dependabot[bot]' && frontend && ( (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == 'PostHog/posthog') \|\| (github.event_name != 'pull_request' && github.repository == 'PostHog/posthog')) | -               |
| `frontend_tests`             | jest, frontend-format, frontend-bundle-size, frontend-typescript-checks          | -                                                                                                                                                                                                                                                    | -               |

</details>
## MCP CI (`ci-mcp.yml`)

**Triggers**: `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run MCP checks"}}
    build["Build Package"]
    integration-tests["Integration Tests"]
    unit-tests["Unit Tests"]
    handle-snapshots("Commit snapshot changes")
    mcp_tests[["MCP Tests Pass"]]

    changes -->|mcp| build
    changes -->|mcp| integration-tests
    changes -->|mcp| unit-tests
    changes -->|mcp| handle-snapshots
    unit-tests --> handle-snapshots
    build --> mcp_tests
    changes --> mcp_tests
    handle-snapshots --> mcp_tests
    integration-tests --> mcp_tests
    unit-tests --> mcp_tests

    class changes gate
    class build test
    class integration-tests test
    class unit-tests test
    class handle-snapshots sideeffect
    class mcp_tests collation
```

<details>
<summary>Job details</summary>

| Job                 | Depends on                                                      | Condition                                                                                                                                                                                                                          | Matrix |
| ------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `changes`           | -                                                               | -                                                                                                                                                                                                                                  | -      |
| `build`             | changes                                                         | mcp                                                                                                                                                                                                                                | -      |
| `integration-tests` | changes                                                         | mcp                                                                                                                                                                                                                                | -      |
| `unit-tests`        | changes                                                         | mcp                                                                                                                                                                                                                                | -      |
| `handle-snapshots`  | changes, unit-tests                                             | mcp && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && !contains(github.actor, '[bot]') && github.actor != 'posthog-bot' && !contains(github.actor, 'github-actions') | -      |
| `mcp_tests`         | changes, build, unit-tests, integration-tests, handle-snapshots | -                                                                                                                                                                                                                                  | -      |

</details>
## Node.js CI (`ci-nodejs.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run Node.js checks"}}
    build["Node.js Build"]
    lint["Node.js Code quality"]
    tests["Node.js Tests (.../3)<br/><i>matrix: shard</i>"]
    node_tests[["Node.js Tests Pass"]]

    changes -->|nodejs| build
    changes -->|nodejs| lint
    changes -->|nodejs| tests
    build --> node_tests
    lint --> node_tests
    tests --> node_tests

    class changes gate
    class build test
    class lint test
    class tests test
    class node_tests collation
```

<details>
<summary>Job details</summary>

| Job          | Depends on         | Condition | Matrix |
| ------------ | ------------------ | --------- | ------ |
| `changes`    | -                  | -         | -      |
| `build`      | changes            | nodejs    | -      |
| `lint`       | changes            | nodejs    | -      |
| `tests`      | changes            | nodejs    | shard  |
| `node_tests` | tests, build, lint | -         | -      |

</details>
## Proto CI (`ci-proto.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`, `workflow_dispatch`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run proto checks"}}
    breaking["Check for breaking changes"]
    lint["Lint proto definitions"]
    python-codegen["Check Python proto stubs are up to date"]
    proto_checks[["Proto Checks Pass"]]

    changes -->|proto| breaking
    changes -->|proto| lint
    changes -->|proto| python-codegen
    breaking --> proto_checks
    changes --> proto_checks
    lint --> proto_checks
    python-codegen --> proto_checks

    class changes gate
    class breaking test
    class lint test
    class python-codegen test
    class proto_checks collation
```

<details>
<summary>Job details</summary>

| Job              | Depends on                              | Condition                              | Matrix |
| ---------------- | --------------------------------------- | -------------------------------------- | ------ |
| `changes`        | -                                       | github.repository == 'PostHog/posthog' | -      |
| `breaking`       | changes                                 | proto                                  | -      |
| `lint`           | changes                                 | proto                                  | -      |
| `python-codegen` | changes                                 | proto                                  | -      |
| `proto_checks`   | changes, lint, breaking, python-codegen | -                                      | -      |

</details>
## Rust CI (`ci-rust.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`, `workflow_dispatch`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run Rust checks"}}
    build["Build Rust services"]
    linting["Lint Rust services"]
    test["Test Rust (...)<br/><i>matrix: dynamic</i>"]
    rust_tests[["Rust Tests Pass"]]

    changes -->|rust| build
    changes -->|rust| linting
    changes -->|rust| test
    build --> rust_tests
    changes --> rust_tests
    linting --> rust_tests
    test --> rust_tests

    class changes gate
    class build test
    class linting test
    class test test
    class rust_tests collation
```

<details>
<summary>Job details</summary>

| Job          | Depends on                    | Condition                              | Matrix  |
| ------------ | ----------------------------- | -------------------------------------- | ------- |
| `changes`    | -                             | github.repository == 'PostHog/posthog' | -       |
| `build`      | changes                       | rust                                   | -       |
| `linting`    | changes                       | rust                                   | -       |
| `test`       | changes                       | rust                                   | dynamic |
| `rust_tests` | changes, build, test, linting | -                                      | -       |

</details>
## Security (`ci-security.yaml`)

**Triggers**: `merge_group`, `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    ensure-pinned-actions["ensure-pinned-actions"]
    semgrep-general["semgrep-general"]
    semgrep-go["semgrep-go"]
    semgrep-js["semgrep-js"]
    semgrep-python["semgrep-python"]
    semgrep-rust["semgrep-rust"]
    semgrep-test-rules["semgrep-test-rules"]
    semgrep_checks[["Semgrep Checks Pass"]]

    semgrep-general --> semgrep_checks
    semgrep-go --> semgrep_checks
    semgrep-js --> semgrep_checks
    semgrep-python --> semgrep_checks
    semgrep-rust --> semgrep_checks
    semgrep-test-rules --> semgrep_checks

    class ensure-pinned-actions test
    class semgrep-general test
    class semgrep-go test
    class semgrep-js test
    class semgrep-python test
    class semgrep-rust test
    class semgrep-test-rules test
    class semgrep_checks collation
```

<details>
<summary>Job details</summary>

| Job                     | Depends on                                                                                | Condition | Matrix |
| ----------------------- | ----------------------------------------------------------------------------------------- | --------- | ------ |
| `ensure-pinned-actions` | -                                                                                         | -         | -      |
| `semgrep-general`       | -                                                                                         | -         | -      |
| `semgrep-go`            | -                                                                                         | -         | -      |
| `semgrep-js`            | -                                                                                         | -         | -      |
| `semgrep-python`        | -                                                                                         | -         | -      |
| `semgrep-rust`          | -                                                                                         | -         | -      |
| `semgrep-test-rules`    | -                                                                                         | -         | -      |
| `semgrep_checks`        | semgrep-python, semgrep-go, semgrep-rust, semgrep-js, semgrep-general, semgrep-test-rules | -         | -      |

</details>
## Storybook (`ci-storybook.yml`)

**Triggers**: `merge_group`, `pull_request`, `push`

```mermaid
graph TD
    classDef gate fill:#4a9eff,stroke:#2563eb,color:#fff
    classDef plumbing fill:#a78bfa,stroke:#7c3aed,color:#fff
    classDef test fill:#34d399,stroke:#059669,color:#fff
    classDef collation fill:#fbbf24,stroke:#d97706,color:#000
    classDef sideeffect fill:#f87171,stroke:#dc2626,color:#fff

    changes{{"Determine need to run storybook checks"}}
    build-storybook["Build Storybook"]
    flake-verification["Storybook flake verification"]
    visual-regression["Visual regression tests - ... (.../...)<br/><i>matrix: include(20)</i>"]
    calculate-running-time(["Calculate running time"])
    handle-snapshots("Handle snapshot changes")
    snapshot-failure-comment("Post snapshot failure guidance")
    snapshot-success-cleanup("Clean up snapshot hint")
    visual_regression_tests[["Visual regression tests pass"]]

    changes -->|frontend| build-storybook
    build-storybook --> flake-verification
    changes -->|frontend| flake-verification
    build-storybook --> visual-regression
    changes -->|frontend| visual-regression
    changes -->|frontend| calculate-running-time
    visual-regression --> calculate-running-time
    changes -->|frontend| handle-snapshots
    visual-regression --> handle-snapshots
    changes --> snapshot-failure-comment
    visual-regression --> snapshot-failure-comment
    changes --> snapshot-success-cleanup
    visual-regression --> snapshot-success-cleanup
    handle-snapshots --> visual_regression_tests
    visual-regression --> visual_regression_tests

    class changes gate
    class build-storybook test
    class flake-verification test
    class visual-regression test
    class calculate-running-time plumbing
    class handle-snapshots sideeffect
    class snapshot-failure-comment sideeffect
    class snapshot-success-cleanup sideeffect
    class visual_regression_tests collation
```

<details>
<summary>Job details</summary>

| Job                        | Depends on                          | Condition                                                                                                                                                                                                                                            | Matrix      |
| -------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `changes`                  | -                                   | github.event_name != 'merge_group'                                                                                                                                                                                                                   | -           |
| `build-storybook`          | changes                             | frontend                                                                                                                                                                                                                                             | -           |
| `flake-verification`       | changes, build-storybook            | frontend && github.event_name == 'pull_request'                                                                                                                                                                                                      | -           |
| `visual-regression`        | changes, build-storybook            | frontend                                                                                                                                                                                                                                             | include(20) |
| `calculate-running-time`   | visual-regression, changes          | github.actor != 'dependabot[bot]' && frontend && ( (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == 'PostHog/posthog') \|\| (github.event_name != 'pull_request' && github.repository == 'PostHog/posthog')) | -           |
| `handle-snapshots`         | visual-regression, changes          | needs.changes.outputs.mode == 'update' && frontend && github.event.pull_request.head.repo.full_name == github.repository                                                                                                                             | -           |
| `snapshot-failure-comment` | visual-regression, changes          | needs.visual-regression.result == 'failure' && needs.changes.outputs.mode == 'check' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository                                                    | -           |
| `snapshot-success-cleanup` | visual-regression, changes          | needs.visual-regression.result == 'success' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository                                                                                             | -           |
| `visual_regression_tests`  | visual-regression, handle-snapshots | -                                                                                                                                                                                                                                                    | -           |

</details>
